# PyOpenARCH

PyOpenARCH is a free/open-source grand strategy game set in a fantasy archipelago. The game is written in python and uses the pygame-ce library.

## Installation

To install and run PyOpenARCH, clone this repository, make sure you have `pygame-ce` installed on your system along with `python 3.13`, and run `main.py`.